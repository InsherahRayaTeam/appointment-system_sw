<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>org.example</groupId>
    <artifactId>appointment-system</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <argLine>

        </argLine>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <junit.version>5.10.2</junit.version>
        <mockito.version>5.2.0</mockito.version>
        <jacoco.version>0.8.13</jacoco.version>

        <sonar.organization>insherahrayateam</sonar.organization>
        <sonar.projectKey>InsherahRayaTeam_appointment-system_sw</sonar.projectKey>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>

        <sonar.coverage.jacoco.xmlReportPaths>
            ${project.build.directory}/site/jacoco/jacoco.xml
        </sonar.coverage.jacoco.xmlReportPaths>

        <!-- Exclude GUI and Main from SonarCloud coverage calculation -->
        <sonar.coverage.exclusions>
            **/Main.java,
            **/main.java,
            **/presentation/gui/**,
            **/gui/**
        </sonar.coverage.exclusions>
    </properties>

    <dependencies>
        <!-- JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Jakarta Mail API -->
        <dependency>
            <groupId>jakarta.mail</groupId>
            <artifactId>jakarta.mail-api</artifactId>
            <version>2.1.3</version>
        </dependency>

        <!-- Mail runtime implementation -->
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-mail</artifactId>
            <version>2.0.4</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Mockito + JUnit 5 integration -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Mockito inline mocking -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <version>5.2.0</version>
            <scope>test</scope>
        </dependency>

        <!-- Byte Buddy for Java 25+ compatibility -->
        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
            <version>1.14.14</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Run JUnit 5 tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <!-- Preserve any argLine set by other plugins (JaCoCo) and append our VM flag -->
                    <argLine>${argLine} -Dnet.bytebuddy.experimental=true</argLine>
                </configuration>
            </plugin>

            <!-- JaCoCo code coverage plugin -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <!-- Exclude GUI and Main from JaCoCo report -->
                    <excludes>
                        <exclude>**/Main.class</exclude>
                        <exclude>**/main.class</exclude>
                        <exclude>**/presentation/gui/**</exclude>
                        <exclude>**/gui/**</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>

                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/site/jacoco</outputDirectory>
                            <formats>
                                <format>XML</format>
                                <format>HTML</format>
                            </formats>
                            <excludes>
                                <exclude>**/Main.class</exclude>
                                <exclude>**/main.class</exclude>
                                <exclude>**/presentation/gui/**</exclude>
                                <exclude>**/gui/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- CI profile: do not run Swing GUI tests in GitHub Actions -->
        <profile>
            <id>ci</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>3.2.5</version>
                            <configuration>
                                <useModulePath>false</useModulePath>
                                <!-- Preserve JaCoCo argLine and append Byte Buddy flag for CI runs -->
                                <argLine>${argLine} -Dnet.bytebuddy.experimental=true</argLine>
                                <excludes>
                                    <exclude>**/presentation/gui/**/*Test.java</exclude>
                                    <exclude>**/presentation/gui/*Test.java</exclude>
                                    <exclude>**/gui/**/*Test.java</exclude>
                                    <exclude>**/gui/*Test.java</exclude>
                                </excludes>
                            </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
